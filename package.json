{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "echo 'No build process needed - serving static files from public/'",
        "build": "echo 'No build process needed - serving static files from public/'"
    },
    "devDependencies": {
        "axios": "^1.11.0"
    }
}